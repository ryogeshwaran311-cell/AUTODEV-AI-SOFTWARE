services:
  - type: web
    name: simple-calculator-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/app.py
    envVars:
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0